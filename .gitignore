# ── Секреты и конфигурация ──────────────────────────────────────────
.env

# ── Python окружение ─────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/

# ── Сгенерированные аудиофайлы (создаются в рантайме) ────────────────
generated_audio/*.wav

# ── Большие бинарные файлы моделей (скачиваются отдельно) ────────────
models/*.onnx
models/*.onnx.json

# ── macOS ────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── IDE ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
